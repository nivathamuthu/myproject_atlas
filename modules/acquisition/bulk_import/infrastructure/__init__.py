"""Infrastructure layer for the Bulk Import module.

The infrastructure layer contains technical implementations required
by the Bulk Import application and domain layers.

Responsibilities may include:
- persistence implementations
- file/object storage implementations
- external provider integrations

Infrastructure code may depend on:
- database libraries
- storage SDKs
- filesystem APIs
- external service clients

However, infrastructure must implement the contracts expected by
the application and domain layers.

Infrastructure details must not leak into domain business logic.
"""
