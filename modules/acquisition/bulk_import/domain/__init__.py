"""Domain layer for the Bulk Import module.

The domain layer contains the core business concepts and rules
associated with bulk importing documents into Project Atlas.

This layer may contain:
- entities
- value objects
- repository contracts
- domain exceptions
- domain business rules

The domain layer must remain independent from infrastructure.

It must not depend directly on:
- databases
- object storage
- filesystem implementations
- cloud-provider SDKs
- HTTP frameworks
- message brokers
- external service clients
"""
